const binary = (array, target) => {
    let arr = array.sort();
    let low = 0;
    let high = arr.length - 1;
    let mid = (low + high) / 2;

    while(low <= high){
        
    }

}

console.log(binary([1, 3, 5, 7, 9], 9));